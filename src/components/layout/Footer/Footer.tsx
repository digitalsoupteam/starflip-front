import type { FC } from 'react';
import { Wrapper } from '../index.ts';

const Footer: FC = () => {
  return (
    <footer>
      <Wrapper>Footer</Wrapper>
    </footer>
  );
};

export default Footer;
